apiVersion: v1
kind: Service
metadata:
  name: mariadb  # Changed to match the selector and app name
  namespace: bookstack-dev
spec:
  selector:
    app: mariadb
  ports:
    - name: mysql
      port: 3306
      targetPort: 3306
    - name: metrics    # <--- ADD THIS
      port: 9104
      targetPort: 9104
  type: ClusterIP